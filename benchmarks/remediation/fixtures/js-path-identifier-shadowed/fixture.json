{
  "schema_version": "v1",
  "id": "js-path-identifier-shadowed-001",
  "repository_id": "fixture-js-docs-shadowed",
  "split": "holdout",
  "family": "path_containment",
  "kind": "negative",
  "expected_state": "unsupported",
  "source": "app.js",
  "finding": {
    "id": "js-path-identifier-shadowed-1",
    "rule_id": "cwe-22.path-traversal-fs",
    "cwe_id": "CWE-22",
    "category": "path traversal",
    "message": "The document name is concatenated onto a base directory and read without a containment check.",
    "path": "app.js",
    "line_start": 10,
    "line_end": 10
  },
  "reason": "The module binds `path` to a string of its own and does not require node:path. The repair writes `path.resolve` and `path.sep`, so an inserted import would be shadowed at the very line the repair rewrites and the check would run against a string. The template refuses as path_identifier_shadowed rather than emitting a repair that does not do what it says.",
  "budget": {
    "max_tool_calls": 20,
    "max_attempts": 3,
    "max_input_tokens": 120000,
    "max_output_tokens": 120000,
    "max_cost_usd": 2.0
  },
  "verification_checks": [
    {
      "check_id": "exploit",
      "kind": "exploit",
      "argv": [
        "node",
        "tests/verify.js",
        "--exploit"
      ],
      "timeout_seconds": 20,
      "expected_baseline": "failed",
      "expected_candidate": "passed"
    },
    {
      "check_id": "behavior",
      "kind": "behavior",
      "argv": [
        "node",
        "tests/verify.js",
        "--behavior"
      ],
      "timeout_seconds": 20,
      "expected_baseline": "passed",
      "expected_candidate": "passed"
    }
  ]
}
