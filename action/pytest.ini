[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    repo_definition: reads repository files the action's image deliberately does not contain
        (the workflow files, the services' own requirement lists). These assert things about
        the repository, not about the image, so the in-image run skips them with
        -m "not repo_definition" and the host job runs everything.
